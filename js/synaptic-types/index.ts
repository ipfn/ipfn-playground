/**
 * @license Apache-2.0
 */

/**
 * Mapping of native synaptic types to it's names.
 */
const SynapticTypes: {
  Bool: string;
  Buffer: string;
  Number: string;
  String: string;
} = {
    Bool: 'bool',
    Buffer: 'buffer',
    Number: 'number',
    String: 'string',
  };

export default SynapticTypes;
